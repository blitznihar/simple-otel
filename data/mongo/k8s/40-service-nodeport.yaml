apiVersion: v1
kind: Service
metadata:
  name: mongo-nodeport
  namespace: simple-otel
spec:
  selector:
    app: mongo
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
      nodePort: 31017
  type: NodePort